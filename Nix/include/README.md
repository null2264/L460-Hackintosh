# Includes

Copy and paste `sample.nix` in the same directory and edit the values accordingly.

MLB, Serial Number and UUID can be generated using [GenSMBIOS](github.com/corpnewt/GenSMBIOS), make sure that the Serial Number is invalid when you put it in [Apple's Check Coverage](https://checkcoverage.apple.com/coverage) site.
